import { StyleSheet } from "react-native";

//Definir estilos globales
export const styles = StyleSheet.create({
    container:{
        margin: 20
    },
    title:{
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: 10
    }
})